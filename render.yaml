services:
  - type: web
    name: siem-backend
    runtime: python
    rootDir: backend
    buildCommand: ./build.sh
    startCommand: gunicorn siem_project.wsgi:application
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: DATABASE_URL
        fromDatabase:
          name: siem-db
          property: connectionString
      - key: FRONTEND_URL
        value: https://siem-frontend.onrender.com  # update after frontend is deployed
      - key: ALLOWED_HOSTS
        value: siem-backend.onrender.com

  - type: web
    name: siem-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: build
    envVars:
      - key: REACT_APP_API_URL
        value: https://siem-backend.onrender.com  # update with your backend URL
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

databases:
  - name: siem-db
    databaseName: siem_db
    plan: free
